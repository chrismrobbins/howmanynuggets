# Local Claude Code settings
.claude/

# Internal design spec — not part of this project
Claude.md

# Unused original image (site uses the transparent nugget.png)
nugget.jpeg

# OS / editor cruft
.DS_Store
*.log

# Verification scaffolding only. The SITE has no dependencies and no build
# step — it is plain <script> tags and runs from disk. `npm i playwright` is
# how the night shift drives a headless browser; none of it ships.
node_modules/
package.json
package-lock.json

# Blender art-department droppings (blender/*.blend ARE tracked — they're source)
__pycache__/
blender/renders/
blender/glb/
blender/*.blend1
blender/render_hall/
blender/contact.png
blender/sheet_preview.jpg

# Verification shots (blender/tools/*.js writes here) — images, not source
blender/tools/_shots/
